# Como es spa le digo que todas las rutas van en la raiz, que son rutas "ficticias" del react router
services:
  - type: web
    name: skylimit-front
    env: static
    buildCommand: npm run build
    publishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
